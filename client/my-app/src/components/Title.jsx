import { useContext } from "react"

//repeating title object
//decorations later?
const Title = (props)=>{

    return (
        <h1>
            {props.title}
        </h1>    
    )
}

export default Title